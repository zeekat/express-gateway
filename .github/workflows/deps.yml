name: Check deps

on:
  schedule:
    - cron: '12 * * * *'

jobs:
  deps:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/setup-node@v1
      with:
        node-version: 14

    - uses: actions/checkout@v2
    - run: npm ci
    - run: npm audit
